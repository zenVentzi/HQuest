type Comment {
  id: ID!
  user: User!
  value: String!
}

type Mutation {
  commentAnswerEdition(
    answerId: ID!
    answerEditionId: ID!
    comment: String!
  ): Comment!
  editComment(
    answerId: ID!
    answerEditionId: ID!
    commentId: ID!
    commentValue: String!
  ): Comment!
  removeComment(answerId: ID!, answerEditionId: ID!, commentId: ID!): Comment!
}
