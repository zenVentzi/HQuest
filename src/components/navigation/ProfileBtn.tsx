import React from "react";
import { UserCircle } from "styled-icons/fa-solid/UserCircle";

import IconBtn from "Reusable/IconBtn";

const ProfileBtn = () => {
  return <IconBtn icon={UserCircle} size="2em" onClick={() => {}} />;
};

export default ProfileBtn;
